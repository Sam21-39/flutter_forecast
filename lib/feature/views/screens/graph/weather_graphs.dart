import 'package:flutter/material.dart';

class WeatherGraphs extends StatelessWidget {
  const WeatherGraphs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
